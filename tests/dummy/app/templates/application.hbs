<h2 id="title">Power-select a time</h2>

selected: {{this.time}}
<TimePicker
  @selected={{this.time}}
  @onChange={{fn (mut this.time)}}
  as |time|>
  {{time}}
</TimePicker>
<br>
selected will be added to options:
<TimePicker
  @selected="06:03"
  @onChange={{fn (mut this.time)}}
  as |time|>
  {{time}}
</TimePicker>