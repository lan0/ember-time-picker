import OptionsComponent from 'ember-power-select/components/power-select/options';
import templateLayout from '../../templates/components/power-time-picker/options';
import { layout, tagName } from "@ember-decorators/component";

export default @tagName('') @layout(templateLayout) class extends OptionsComponent {
}
